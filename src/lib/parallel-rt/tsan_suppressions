# ThreadSanitizer suppressions file for the runtime system

# Usage: TSAN_OPTIONS="suppressions=/path/to/this/file" ./a.out

# these tests are added by -debug flag and can be ignored
race:CheckGlobalAddr

# ignore races in libnuma
race:libnuma
