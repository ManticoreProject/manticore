#ifndef _POR_DEF_
#define _POR_DEF_

typedef por_fun = fun(unit / exh -> option);

define @p-or (f1 : por_fun, f2 : por_fun / exh : exh) : option;

typedef por_state = any;

#define POR_EMPTY      enum(0)
#define POR_DONE       enum(1)
#define POR_FULL       enum(2)

#endif /*! _POR_DEF_ */
