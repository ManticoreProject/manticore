(* itos.hlop
 *
 * COPYRIGHT (c) 2007 The Manticore Project (http://manticore.cs.uchicago.edu)
 * All rights reserved.
 *
 * HLOp wrapper for long to string conversion
 *)

#include "types.def"

extern void *M_IntToString (long) __attribute__((alloc,pure));

define inline @itos (n : ml_int / exh : exh) : ml_string =
    let res : ml_string = ccall M_IntToString (unwrap(n))
      return (res)
;
