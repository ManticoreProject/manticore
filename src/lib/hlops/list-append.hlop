(* list-append.hlop
 *
 * COPYRIGHT (c) 2007 The Manticore Project (http://manticore.cs.uchicago.edu)
 * All rights reserved.
 *
 * Builtin list append function.
 *)

#include "types.def"

define @list-append (l1 : list, l2 : list) : list =
    fun append (l1 : list) : list =
	  case l1
	   of CONS(hd:any, tl:list) => CONS(hd, apply append (tl))
	    | NIL => l2
	  end
      apply append (l1)
;
