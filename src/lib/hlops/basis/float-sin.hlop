(* float-sin.hlop
 *
 * COPYRIGHT (c) 2007 The Manticore Project (http://manticore.cs.uchicago.edu)
 * All rights reserved.
 *)

#include "types.def"

extern float sinf (float) __attribute__((pure));

define @float-sin (x : ml_float / exh : exh) : ml_float =
    let y : float = ccall sinf (unwrap(x))
    let res : ml_float = wrap(y)
      return (res)
;
