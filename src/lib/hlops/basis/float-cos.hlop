(* float-cos.hlop
 *
 * COPYRIGHT (c) 2007 The Manticore Project (http://manticore.cs.uchicago.edu)
 * All rights reserved.
 *)

#include "types.def"

extern float cosf (float) __attribute__((pure));

define @float-cos (x : ml_float / exh : exh) : ml_float =
    let y : float = ccall cosf (unwrap(x))
    let res : ml_float = wrap(y)
      return (res)
;
