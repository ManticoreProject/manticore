(* array-length.hlop
 *
 * COPYRIGHT (c) 2008 The Manticore Project (http://manticore.cs.uchicago.edu)
 * All rights reserved.
 *
 * Get the length of an array.
 *)

#include "array.def"

define @array-length (arr : array / exh : exh) : ml_int =
  let len : int = ArrayLength(arr)
  let wlen : ml_int = wrap(len)
  return(wlen)
;
