(* string-size.hlop
 *
 * COPYRIGHT (c) 2007 The Manticore Project (http://manticore.cs.uchicago.edu)
 * All rights reserved.
 *
 * Get the size of an ML string.
 *)

#include "types.def"

define inline @string-size (s : ml_string) : ml_int =
    let len : int = #1(s)
    let res : ml_int = [len]
      return (res)
;
