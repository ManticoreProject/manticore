#! /bin/bash

CM=sml

if [ ! -d .heap ]
then 
    mkdir .heap
fi

$CM <<XXX
   CM.make("sources.cm");
   SMLofNJ.exportFn
	(".heap/manticore", 
 	 fn (_, [file]) => 
	       (TestCompile.init file; OS.Process.success)):unit;
XXX
