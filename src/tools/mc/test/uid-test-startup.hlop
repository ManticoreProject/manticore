#include "types.def"
#include "debugging.def"
#include "fgs.def"
#include "assert.def"

define @uid-test-startup ( / exh : exh) : () = 
  do @default-scheduler-startup(/exh)

  let i1 : int = @uid(tag(uid), host_vproc / exh)
   do ccall M_PrintInt(i1)
  let i2 : int = @uid(tag(uid), host_vproc / exh)
   do ccall M_PrintInt(i1)
  let i3 : int = @uid(tag(uid), host_vproc / exh)

  return()
;
