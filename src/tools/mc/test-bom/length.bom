(* length.bom *)

module Length
  fun init (arg : any; exh : cont(any)) : unit =
    fun length (l : list; exh : cont(any)) : int =
	  case l
	   of enum(0) => return (0:int)
	    | CONS(_, tl:list) =>
		let n:int = apply length(tl; exh)
		return (I32Add(n, 1))
	  end
    return (enum(0))
