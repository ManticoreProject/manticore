(* logging.pml
 *
 * COPYRIGHT (c) 2009 The Manticore Project (http://manticore.cs.uchicago.edu)
 * All rights reserved.
 *
 * HLOps for logging events
 *)

#include "log-events.def"

structure Logging =
  struct

    _primcode (

#ifdef ENABLE_LOGGING

	define @NextLogEvent (vp : vproc) : addr(any) =
(* FIXME *)
	  ;

      (* fill in the timestamp field of the event *)
	define @LogTimestamp (ep : addr(any)) : () =
(* FIXME *)
	  ;

@GENERIC-LOG-HLOPS@

@LOG-HLOPS@

#else /* !ENABLE_LOGGING */

@DUMMY-LOG-HLOPS@

#endif

@WRAPPED-LOG-HLOPS@

      )

@LOG-FUNCTIONS@

  end
