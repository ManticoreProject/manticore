(* locked-queue-local.hlop
 *
 * COPYRIGHT (c) 2007 The Manticore Project (http://manticore.cs.uchicago.edu)
 * All rights reserved.
 *
 * Imports for the locked queue.
 *)

#ifndef _LOCKED_QUEUE_LOCAL_
#define _LOCKED_QUEUE_LOCAL_

(* spin lock *)
#include "../synch/spin-lock-fn.hlop"
define_spin_lock(lq, locked_queue, LOCKED_QUEUE_LOCK)

(* in-place queue for queue elements *)
#include "in-place-queue-fn.def"
define_in_place_queue(lq, locked_queue, LOCKED_QUEUE_HD, LOCKED_QUEUE_TL)

(* in-place queue for blocked threads *)
define_in_place_queue(bq, locked_queue, LOCKED_QUEUE_BLOCKED_HD, LOCKED_QUEUE_BLOCKED_TL)

#endif /*!  _LOCKED_QUEUE_LOCAL_ */
